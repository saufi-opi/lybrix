VERDICT: APPROVE

Re-review of PLAN.md revision 5. All five revision-4 issues are resolved, and each fix was checked against the actual code it touches:

1. **Rebind vs. non-goal (was contradictory)** — resolved cleanly: §1 decision 2 and the sharpened Non-goals now state "no automated background re-embed migration runner in v1" while explicitly permitting the rebind endpoint as a metadata sync; item 17 and §5's resolution rules repeat the same framing (re-embed via existing per-doc endpoints), and the collections-page copy (item 28) now mentions rebinding. The spec is internally consistent in one direction.

2. **RAM-buffered ingestion (was up to 512 MiB in API heap)** — resolved with a coherent disk-streaming design: temp file + `io.TeeReader` digest, PDF early stop during the download at `MaxDocumentPages*40KB` (~32 MB), EPUB hard ceiling 64 MiB enforced by byte accounting mid-stream, and the 512 MiB figure dropped. Feasibility verified: `pipeline.PageCount(path)` exists (`internal/pipeline/pdfcpu.go:20`), so the post-download `pdfPageCount(tempFile)` works without buffering; `manager.Uploader` is already constructed in `internal/objectstore` (lines 32/51) for the >32 MiB multipart branch.

3. **Pre-flight-only SSRF guard (was bypassable)** — resolved properly: enforcement moved to dial time with a `DialContext` validating every resolved IP before connect plus a `Control` hook re-checking the actual connected address (defeats DNS rebinding), and one shared client whose `CheckRedirect` keeps the same transport so every redirect hop is dial-checked too. Criterion 9 now tests both the dial-time rejection and a redirect-to-private-address case.

4. **EPUB contract vs. `HandleSplit` (was hand-wavy)** — resolved with an explicit per-stage contract that matches the real code: verify-time mime gate (`application/epub+zip`, not the schema default `application/pdf` at `schema.sql:31`), splitter branch on `doc.MimeType` skipping `pipeline.PageCount`/`ExtractBookmarks` with one synthetic `idx=0, page_start=1, page_end=1` shard (matches `internal/service/splitter.go:55-95` structure), docling-only parse routing (anydoc skipped), and a retry ladder that never spawns sub-shards. Confirmed against `LadderAction` (`internal/service/parser.go:24-33`): a span-1 shard already fails both split conditions and falls through to `text_only` — so the plan's "no page-halving" rule is actually the existing behavior, not new machinery; criterion 11 asserts exactly this end-to-end.

5. **Bare `ix_chunks_hnsw` (was silently superseded)** — resolved: §3 now documents the deletion explicitly (with the schema.sql:98 reference), the DO block drops it unconditionally via a `pg_class` check independent of the typmod branch (covering already-converged DBs), criterion 15 asserts both the converged-DB drop and clean fresh-DB boot, and step 1 carries the deletion into both schema copies.

Cross-checks on the new/changed surface: the contract table matches the handler spec (`/v1/models/test` response shape now matches `Probe`'s returns); path arithmetic still holds (7 new paths → 25); the scope table (6 admin ops + 3 ingest ops) matches route registration and the `TestRouteScopeTable` rows; the step sequence (0–14) includes the EPUB branch as its own step with the right files. Two minor implementation notes, neither blocking (both are self-correcting via tests the plan itself adds): (a) in `routeScope`, the new `POST /v1/documents/fetch-url` → "ingest" case must be placed **above** the `matchDocSub(path, "")` → "search" case, which would otherwise swallow it — the added scope-table row will fail loudly if the ordering is wrong; (b) for fetch-url/OPDS bodies there is no client-supplied hash, so the sha-mismatch outcome only applies to paths that carry one — the dedup digest is simply computed for `FindDuplicate`, which is what the plan describes.

The plan is implementable as written: edits match reality, acceptance criteria are testable, contracts are snapshot-consistent, and the two-plane, state-of-record, and shard-unit constraints from CLAUDE.md are preserved throughout.
