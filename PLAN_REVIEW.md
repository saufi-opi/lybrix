VERDICT: APPROVE

Reviewer: verifier · Date: 2026-09-21 · Repo HEAD: fe67823 · Branch: feat/lybrix-2.0-revamp
Scope: re-review of the revised PLAN.md against the five issues raised in the first review round, plus a confirmation pass on blueprint fidelity and repo-state accuracy.

## Resolution of the five prior issues — all fixed

1. **`byte_size` column name — RESOLVED.** §1.3 now names the column `byte_size BIGINT` with an explicit note that it matches the 1.0 schema and `DocumentOut.byte_size` in the OpenAPI contract (PLAN.md:91-92), and `author` is now in the column list. The blueprint's `file_size` rename is correctly rejected. (Citation nit only: the note says schemas.py:38; the field is at schemas.py:36 — cosmetic, no action needed.)
2. **Python eval harness deletion — RESOLVED.** §4.2 step 7 now deletes "the full `scripts/eval/*.py` harness" (`run.py`, `judge.py`, `compare.py`, `dataset.py`, `filters.py`, `mcp_client.py`, `__init__.py`) while explicitly keeping `datasets/seed.jsonl`, `results/`, and the rewritten README (PLAN.md:331, 349). §5 adds a hard zero-Python verification: `find . -name '*.py' -not -path './services/web/node_modules/*'` returning nothing. The zero-custom-Python constraint is now enforced, not just intended.
3. **S3 key layout — RESOLVED.** §1.4 `internal/objectstore/s3.go` now states the verified layout: raw key `{doc_id}.pdf` in bucket `raw`, parsed key `{doc_id}/{shard_idx}.md` in bucket `parsed`, with an explicit "NO `raw/` or `parsed/` path prefix on the keys" note (PLAN.md:144-148) — matching `libs/core/src/core/storage/s3.py:26-33`. (Citation nit only: the plan cites s3.py:31-38; the functions span 26-33.)
4. **`MAX_PARSE_BACKLOG` default — RESOLVED.** §1.4 now carries `MAX_PARSE_BACKLOG=2000 (code default, matching config.py:116; the compose api service overrides it to 20000 — keep that override on the lybrix-server core service)` (PLAN.md:114-116). The companion case `EMBED_BATCH_SIZE=48` code default vs compose-16 override is handled the same way (PLAN.md:109-110). Go config now mirrors 1.0 code defaults with compose overrides preserved.
5. **MinIO profile placement — RESOLVED.** §1.2 adds an explicit decision: MinIO stays on the **ingest** profile as in 1.0, with the reason (parsers download full PDFs per shard; core reaches it over tailscale via `S3_PUBLIC_ENDPOINT`) and the standing rule "every acceptance step that needs object storage: use `make up-core && make up-ingest`, not `make up-core` alone" (PLAN.md:83). All affected acceptance criteria were updated consistently: Phase 1 (PLAN.md:158), Phase 2 (PLAN.md:218), Phase 3 E2E (PLAN.md:313, ingest profile), Phase 4 live E2E (PLAN.md:355), and the rollout sequence starts `up-ingest` before re-ingestion (PLAN.md:327-329).

## Hard-constraint confirmation (unchanged from first pass, still satisfied)

- **100% Go rewrite, zero custom Python:** one `lybrix-server` binary (serve/splitter/parser/embedder/janitor/keys bootstrap/migrate subcommands) + `cmd/lybrix-eval`; Phase-4 delete list now provably empties the repo of Python.
- **ParadeDB replaces Qdrant:** §1.2 removes `qdrant` from compose; §1.3 schema unifies metadata + `vector(1024)` + `paradedb.create_bm25` with the run-once guard for the assertion-based pg_search `CALL`; blueprint §5 RRF SQL carried verbatim into `internal/store/search.go` with the R-14 key-scope filter correctly pushed into **both** CTEs (never post-filter).
- **AnyDoc CGO + docling-serve fallback:** `third_party/anydoc-go` extern-C layout, `LockOSThread` thread-safety, `-tags anydoc` compile-time selection with a `!anydoc` stub so CI builds clean, two-tier yield check (≥50 chars/page), shard-level units only — all faithful to blueprint §4.
- **16–24 page sharding preserved:** SHARD_PAGES=20, splitter ports incl. the mandatory anti-loop guard and degenerate-outline fallback, retry ladder (2 quarter-split / 3 single-page / ≥4 text-only), parent→skipped + sub-shard continuation, `PARSER_RECYCLE_AFTER`, PEL-as-attempt-counter with janitor delivery cap `max(5, MAX_SHARD_ATTEMPTS+1)` — verified against `runner.py`, `janitor.py:178-240`, `parser.py:44-98`, `libs/parsing/.../splitter.py` in round 1 and unchanged.
- **Parent-child retrieval:** 384/64 child windows under 2048–4096-token parents, breadcrumb prefix applied at embed time (stored text stays clean, chunk_hash stable), per-window page ranges (R-13).
- **mcp-go at :8430/mcp:** 6 tools (verified `mcp_server/server.py:261-315`), verbatim tool descriptions, bearer 401 parity, `/health` unauthenticated, per-method usage rows.
- **Testing across all components:** per-phase test inventories map to real 1.0 test files (`tests/test_ack_trim.py`, `test_splitter.py`, `test_chunking.py`, `test_stitch.py`; `services/workers/tests/test_janitor_{dlq,embed_sweep,hygiene}.py`, `test_runner_recycle.py`, `test_retry_ladder.py`, `test_embedder_batches.py` — all present in the repo), testcontainers ParadeDB lane for store tests, miniredis for queue tests, openapi golden test pinning the Go server to the checked-in 18-path contract, and end-to-end acceptance including resilience drills (kill-parser, Redis wipe, poison job → DLQ).

## Readiness

PLAN.md is ready for implementation. The document is internally consistent, its factual claims now match the repo at `fe67823`, every deviation from the blueprint is flagged as a deliberate delta with rationale, and the hard constraints are each traceable to a concrete work item with an acceptance gate. Remaining trivial line-number drift in two code citations (schemas.py:38→36, s3.py:31-38→26-33) does not affect any implementer decision and may be corrected opportunistically. Per the task brief, no git commit or push was made.
