VERDICT: APPROVE

Round-2 re-review of the revised PLAN.md (2026-09-21). All 5 round-1 issues are resolved, verified against the revised text and the actual repo at HEAD (`937b9a1`):

1. **Alembic revision collision — RESOLVED.** Phase 6 now names `migrations/versions/0004_shard_done_at.py` with `down_revision = "0003_api_key_expiry_usage"`, and the plan states the verified chain (`0001_initial → 0002_parsed_uri_md → 0003_api_key_expiry_usage`, matching migrations/versions/0002_parsed_uri_md.py:17-18 and 0003_api_key_expiry_usage.py:19-20). `alembic upgrade head` will work.
2. **`overlap=0` — RESOLVED.** The `_split_and_requeue` snippet now calls `parsing_splitter_fixed_bounds(span, shard_pages, overlap=0)` with an inline comment explaining the disjoint-tiling rationale, and the parenthetical note below it now says "always with `overlap=0`". The snippet and its note no longer contradict each other, and plan test 1 (span-20 → 4 disjoint sub-shards of 5 pages) will pass.
3. **Browser auth mechanism — RESOLVED.** Phase 7b now routes browser calls to the gated endpoints through the existing `/api/admin/v1/*` same-origin proxy (verified: the handler at services/web/app/api/admin/[...path]/route.ts supports GET and POST, verifies the session cookie, and attaches `Authorization: Bearer $API_ADMIN_KEY` upstream — no new proxy code needed, as the plan says). It correctly identifies the breaking callers (`"use client"` documents/collections pages via `useDocuments`/`useCollections`, detail pages via `queries.ts`), keeps direct `API_URL` paths for `!isBrowser`, documents the `API_ADMIN_KEY` dual-scope requirement (`.env.example`), and adds a per-caller audit for server-side readers. Verification includes the documents + collections pages rendering logged-in.
4. **CLAUDE.md drift + `down-ingest` comment — RESOLVED.** Phase 7a now edits `CLAUDE.md:31-32` in the same commit as README.md (the Commands block indeed documents both phantom targets — verified at CLAUDE.md lines 31-32). The replacement comment now says "stops consumers immediately", which is what `docker compose --profile ingest down` actually does, with an explicit note that drain semantics are a separate target and out of scope.
5. **MCP prefix call-site — RESOLVED.** Phase 7e now places the edit directly at server.py:54 inside `search_impl` (`dense = query_embedder(f"{settings.embed_query_prefix}{query}")`), which matches the verified signature (`search_impl` receives `settings: Settings`) and explicitly warns against editing the tool-wiring lambda at server.py:274.

**No new errors introduced.** PART 1 verdicts are byte-identical to round 1 (all previously verified correct). Phase 4's test text was refined (10-line single-section body, `page_start < section_end`) — consistent with the `_sections`/`_emit` mechanics I verified at hybrid.py. Phase 5's plan remains internally consistent; Phase 7b's claims about the proxy and call paths match the actual web code. All acceptance criteria remain real testable commands mapped to the correct suites per CLAUDE.md's root-vs-workers split. Scope is unchanged: no work beyond the review's validated findings.

Remaining non-blocking observations (no revision required):
- PLAN.md's PART 1 cites `events.py:34-79` where the stream route actually sits at :41-81, and `config.py:5` where the no-hardcoding docstring is at :3-4 — trivial line offsets, immaterial.
- Phase 1a's new `409` on zero failed shards is a deliberate behavior change from the prior silent 200 — reasonable, in scope for R-11, but worth noting for the changelog.
- Phase 6's `chunks_embedded` line is still a `...` placeholder with the pattern described in prose; acceptable at plan granularity.
- Phase 6's `write_metrics_rollup` runs inside `janitor_pass`'s transaction — a Redis `queue_depth` failure inside the dict-comprehension would abort the whole janitor pass unless the promised try/except → `{}` is actually implemented; the parenthetical already flags this, so the implementer just must not skip it.
