# rag-platform env template — copy to .env and edit (PRD §13.4 first boot).

# -- control plane -------------------------------------------------------
POSTGRES_PASSWORD=change-me
MINIO_ROOT_USER=rag
MINIO_ROOT_PASSWORD=change-me-too
QDRANT_API_KEY=change-me-as-well

# -- embedding plane -----------------------------------------------------
EMBED_MODEL=BAAI/bge-m3
# LOG_LEVEL=info

# -- ingress (Caddy) -----------------------------------------------------
PUBLIC_HOST=rag.example.com
ACME_EMAIL=ops@example.com
