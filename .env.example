# rag-platform env — split-host deployment (VM2 core / nsspq ingest).
# Copy to .env on BOTH hosts; values identical except nothing — same file works.

# -- identity ------------------------------------------------------------
RAG_TAILSCALE_IP=100.109.176.118        # VM2 — where stores/api/mcp publish
TEI_QUERY_PUBLIC_URL=http://100.80.1.117:8082   # nsspq — tei-query for VM2's mcp

# -- control plane -------------------------------------------------------
POSTGRES_PASSWORD=change-me
MINIO_ROOT_USER=rag
MINIO_ROOT_PASSWORD=change-me-too
QDRANT_API_KEY=change-me-as-well
MCP_API_KEY=openssl-rand-hex-32

# -- object storage ------------------------------------------------------
# Tailscale URL so presigned PUTs work from any tailnet host (and the UI).
S3_PUBLIC_ENDPOINT=http://100.109.176.118:9000

# -- admin UI (browser side) --------------------------------------------
NEXT_PUBLIC_API_URL=http://100.109.176.118:8000

# -- embedding plane -----------------------------------------------------
EMBED_MODEL=BAAI/bge-m3
# LOG_LEVEL=info
